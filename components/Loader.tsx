const Loader = () => {
  return (
    <div className="flex items-center justify-center space-x-2">
      <span className="size-4 bg-blue-500 rounded-full animate-bounce [animation-delay:-0.4s]"></span>
      <span className="size-4 bg-blue-500 rounded-full animate-bounce [animation-delay:-0.2s]"></span>
      <span className="size-4 bg-blue-500 rounded-full animate-bounce"></span>
    </div>
  )
}

export default Loader
